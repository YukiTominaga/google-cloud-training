{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/*.py) ;; *) exit 0 ;; esac; cd \"$CLAUDE_PROJECT_DIR\" && uv run --quiet ruff format --quiet \"$f\" && uv run --quiet ruff check --fix --quiet \"$f\" 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ruff でフォーマット中..."
          }
        ]
      }
    ]
  }
}
